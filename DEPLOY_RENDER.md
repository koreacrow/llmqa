# Render 단독 배포 안내

이 프로젝트는 Render의 단일 Web Service에서 화면과 API를 함께 제공합니다. 작성 기록과 검토 설정은 Persistent Disk에 보관됩니다.

## 1. 배포 전 준비

1. 프로젝트 전체를 GitHub 저장소에 올립니다.
2. Render에 로그인한 뒤 **New + → Blueprint**를 선택합니다.
3. GitHub 저장소를 연결하고 `render.yaml` 설정을 확인합니다.
4. 배포를 시작합니다.

## 2. 저장 데이터 유지 확인

배포 설정에는 다음 값이 포함되어 있습니다.

- 디스크 연결 위치: `/var/data`
- 데이터베이스 환경 변수: `DB_PATH=/var/data/app.db`

이 값이 유지되어야 검토 기록과 AI 검토 기준이 재배포 뒤에도 남습니다. Render 화면에서 디스크가 Web Service에 정상 연결됐는지 확인하세요.

## 3. 배포 상태 확인

배포가 끝나면 다음 순서로 확인합니다.

1. 서비스 첫 화면이 표시되는지 확인합니다.
2. 검토 기준을 하나 변경한 뒤 새로고침해도 유지되는지 확인합니다.
3. 문서 초안을 입력하고 **AI 검수하기**를 눌러 실제로 검토 항목이 채워지는지 확인합니다. (ANTHROPIC_API_KEY가 없으면 500 에러가 표시됩니다.)
4. 섹션 초안을 입력하고 **검토 기록 저장**을 눌러 저장 성공 메시지를 확인합니다.
5. 서비스 상태 확인 주소의 `/api/health` 응답이 `{"ok":true}`인지 확인합니다.

## 4. 개인 도메인 연결

1. Render 서비스의 **Settings → Custom Domains**에서 보유 도메인을 추가합니다.
2. Render가 안내하는 DNS 레코드 값을 도메인 구매 서비스의 DNS 관리 화면에 입력합니다.
3. 인증서 발급과 연결이 완료될 때까지 기다립니다.

처음에는 루트 도메인 또는 `www` 중 하나를 기준 주소로 정하고, 다른 주소는 리디렉션하도록 설정하는 것이 좋습니다.

## 5. LLM API 키 보관 원칙

이 프로젝트는 백엔드(`backend/main.py`)에서 Anthropic Claude API(`claude-sonnet-4-6`)를 직접 호출합니다. API 키는 GitHub, 소스 코드, 브라우저 화면, 배포 설정 파일에 넣지 마세요. Render 서비스의 **Environment** 항목에만 등록하세요.

등록해야 하는 환경 변수:

```text
ANTHROPIC_API_KEY=실제_비밀_키
```

`render.yaml`에는 `ANTHROPIC_API_KEY`가 `sync: false`로 선언되어 있어 저장소에 값이 커밋되지 않습니다. Render 대시보드의 Environment 탭에서 값을 직접 입력해야 합니다.

브라우저는 키를 직접 사용하지 않고, 항상 서버의 `/api/review` 경로에 요청합니다. 서버가 Claude와 통신하고 필요한 결과만 화면으로 돌려주는 방식입니다.

## 6. 운영 전 점검

- API 키가 브라우저 코드나 커밋 기록에 없는지 확인합니다.
- 요청 길이와 호출 횟수에 제한을 둡니다.
- LLM 오류나 시간 초과 시 사용자에게 재시도 안내를 표시합니다.
- 개인 정보, 비공개 기획 문서 등 민감한 내용의 외부 전송 여부를 서비스 정책에 명시합니다.
- 실서비스 전에는 사용자 인증, 요청별 사용량 관리, 로그의 민감 정보 마스킹을 추가하세요.
